# Local development override — exposes Postgres on localhost only.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d postgres
services:
  postgres:
    ports:
      - '127.0.0.1:5432:5432'
